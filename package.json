{
  "name": "limit-monitor",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "lint": "vp lint deploy.ts deploy/provider-config.ts test/provider-config.test.ts && npm run lint --workspaces --if-present",
    "format": "vp fmt package.json deploy.ts deploy/provider-config.ts test/provider-config.test.ts --write && npm run format --workspaces --if-present",
    "format-check": "vp fmt package.json deploy.ts deploy/provider-config.ts test/provider-config.test.ts --check",
    "tsc": "tsc --build tsconfig.json",
    "test:deploy": "node --experimental-strip-types --test test/provider-config.test.ts",
    "test": "npm run test:deploy && npm run test --workspaces --if-present",
    "predev:dashboard": "npm run build -w shared",
    "dev:dashboard": "npm run dev -w dashboard",
    "predev:hub": "npm run build -w shared",
    "dev:hub": "node --watch dist/src/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "vite-plus": "^0.1.24"
  }
}